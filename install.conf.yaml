- clean:
    - "~"
    - "~/bin"

- link:
    ~/.ssh/config: ssh-config
    ~/.gitconfig:
    ~/.zshrc:
      force: true
    ~/bin:
      glob: true
      create: true
      path: bin/*

- create:
    - ~/src
